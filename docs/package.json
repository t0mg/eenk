{
  "name": "eenk-docs",
  "version": "1.0.0",
  "description": "Documentation site builder for eenk",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch && python -m http.server -d dist"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}